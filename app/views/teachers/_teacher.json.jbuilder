json.extract! teacher, :id, :name, :father, :education, :phone, :address, :cnic, :date_of_joining, :date_of_leaving, :increment, :monthly_salary, :created_at, :updated_at
json.url teacher_url(teacher, format: :json)
