<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <button class="btn btn-primary" id="menu-toggle">menu</button>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class: "nav-link" %>
        <!-- <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a> -->
      </li>
      <!--<li class="nav-item">
        <a class="nav-link" href="#">Link</a>
      </li> -->
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" aria-haspopup="true" aria-expanded="false" onclick="toggle_dropdown(); return false;">
          <%= @school_branch.name %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= link_to 'Branches', school_branches_path, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= link_to 'Active Branch', school_branch_path(@school_branch), class: "dropdown-item" %>
        </div>
      </li>
    </ul>
  </div>
</nav>

<script>
function toggle_dropdown() {
  $('.dropdown-menu').toggle();
}

$(document).ready(function(){
  $('.container-fluid').on("click", function(){
    $('.dropdown-menu').hide()
  });
});
</script>
