<%= form_for(class_section) do |f| %>
  <% if class_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_section.errors.count, "error") %> prohibited this class_section from being saved:</h2>

      <ul>
      <% class_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level_id %>
    <%= f.text_field :level_id %>
  </div>

  <div class="field">
    <%= f.label :section_id %>
    <%= f.text_field :section_id %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
